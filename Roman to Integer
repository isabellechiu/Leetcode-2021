class Solution:
    def romanToInt(self, s: str) -> int:
        d = {'I':1,'V':5,'X':10,'L':50,'C':100,'D':500,'M':1000}
        l = [d[i] for i in s]
        for i in range(0, len(l)-1):
            if l[i]<l[i+1]:
                l[i] = -l[i]     
        return sum(l)
